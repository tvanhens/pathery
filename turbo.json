{
  "pipeline": {
    "build:lambda": {
      "outputs": ["target/"]
    },
    "synth": {
      "dependsOn": ["^build:lambda"],
      "outputs": ["cdk.out"]
    },
    "deploy": {
      "dependsOn": ["^build:lambda"]
    }
  }
}
